# ── Core Framework ────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18
python-dotenv==1.0.1

# ── Database ──────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.3.6

# ── Task Queue ────────────────────────────────────────────
celery[redis]==5.4.0
redis==5.2.0
flower==2.0.1                    # Celery monitoring UI

# ── ML / OCR Models ──────────────────────────────────────
torch==2.7.0
torchvision==0.22.0
transformers==4.52.3
datasets==3.6.0
Pillow==11.2.1
opencv-python==4.11.0.86
numpy>=1.26.0,<2.1          # evidently requires numpy<2.1
accelerate==0.34.2          # required for HuggingFace Trainer
evaluate==0.4.3             # required for computing CER/WER
jiwer==3.0.4                # required by evaluate for CER/WER

# ── Embeddings ────────────────────────────────────────────
sentence-transformers==3.3.1

# ── MLOps ─────────────────────────────────────────────────
mlflow==2.22.0
evidently>=0.6.0            # numpy<2.1 constraint — pip will resolve compatible version

# ── Agent / LLM ───────────────────────────────────────────
langchain==0.3.25
langgraph==0.4.8
# DeepSeek (OpenAI-compatible) is the agent-layer LLM provider.
# Pin to the 0.3.x line to stay compatible with langchain-core 0.3.x.
langchain-openai>=0.3,<0.4
openai>=1.104.0
# Gemini retained only for the LayoutLMv3 vision-OCR bounding-box fallback.
langchain-google-genai>=2.0.0
google-generativeai>=0.8.0
# Required by the Donut tokenizer (XLMRoberta/sentencepiece-based).
sentencepiece>=0.2.0

# ── PDF Generation ────────────────────────────────────────
reportlab==4.2.5

# ── Validation & Safety ──────────────────────────────────
sqlparse==0.5.3
pydantic==2.11.5
pydantic-settings==2.9.1

# ── Utilities ─────────────────────────────────────────────
httpx==0.28.1
websockets==15.0.1
aiofiles==24.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
